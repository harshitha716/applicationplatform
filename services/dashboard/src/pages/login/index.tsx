'use client';
import React from 'react';
import { LoginForm } from 'modules/login/LoginForm';
// import LoginForm from 'modules/login/LoginForm';
import type { NextPage } from 'next';

const Home: NextPage = () => {
  return <LoginForm />;
};

export default Home;
