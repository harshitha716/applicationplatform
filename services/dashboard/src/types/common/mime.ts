export enum INPUT_FILE_FORMATS {
  PNG = 'image/png',
  JPEG = 'image/jpeg',
  JPG = 'image/jpg',
  DOCX = '.docx',
  PDF = '.pdf',
  BMP = '.bmp',
  CSV = '.csv',
  XLSX = '.xlsx',
  XLS = '.xls',
  BAI2 = '.bai2',
}

export enum FILE_EXTENSION {
  CSV = 'csv',
  XLSX = 'xlsx',
  BAI2 = 'bai2',
}
